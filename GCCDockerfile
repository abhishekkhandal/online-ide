FROM gcc:7.3
ENV ENV="/etc/profile"
COPY how /usr/local/bin
WORKDIR /home
RUN apt-get update && \
apt-get -y install nano vim libc6-dev && \
rm -rf /var/lib/apt/lists/* && \
echo "how" > "$ENV"
ENTRYPOINT "how" && /bin/bash
